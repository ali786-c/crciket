<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3"><div><p class="cricket-kicker mb-2">Security governance</p><h1 class="display-6 fw-bold mb-2">Global audit explorer</h1><p class="text-secondary mb-0">Search the complete platform trail by actor, action, tournament, IP address, or date range.</p></div><div class="d-flex gap-2"><a href="{{ route('super-admin.audit-logs.export', request()->query()) }}" class="btn btn-success"><i class="fa-solid fa-download me-2"></i>Export CSV</a><a href="{{ route('super-admin.dashboard') }}" class="btn btn-light">Control plane</a></div></div></x-slot>
    <div class="container pb-5">
        <div class="cricket-surface p-4 mb-4"><form method="GET" class="row g-2 align-items-end"><div class="col-lg-4"><label class="form-label small text-secondary">Search</label><input type="search" name="search" value="{{ $search }}" class="form-control" placeholder="Action, actor, email, tournament, or IP"></div><div class="col-lg-3"><label class="form-label small text-secondary">Action</label><select name="action" class="form-select"><option value="">All actions</option>@foreach($actions as $action)<option value="{{ $action }}" @selected($selectedAction === $action)>{{ $action }}</option>@endforeach</select></div><div class="col-lg-2"><label class="form-label small text-secondary">From</label><input type="date" name="from" value="{{ $from }}" class="form-control"></div><div class="col-lg-2"><label class="form-label small text-secondary">To</label><input type="date" name="to" value="{{ $to }}" class="form-control"></div><div class="col-lg-1 d-flex gap-2"><button class="btn btn-success flex-grow-1"><i class="fa-solid fa-filter"></i></button></div><div class="col-12"><a href="{{ route('super-admin.audit-logs.index') }}" class="small text-secondary">Reset filters</a></div></form></div>
        <div class="cricket-surface overflow-hidden"><div class="d-flex justify-content-between align-items-center gap-3 p-4 border-bottom"><div><p class="cricket-kicker mb-1">Immutable operational trail</p><h2 class="h4 fw-bold mb-0">Audit events</h2></div><span class="small text-secondary">{{ $logs->total() }} matching events</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Time</th><th>Action</th><th>Actor</th><th>Scope</th><th>Network</th><th>Metadata</th></tr></thead><tbody>@forelse($logs as $log)<tr><td class="text-secondary text-nowrap">{{ $log->created_at?->format('d M Y H:i:s') }}</td><td><span class="badge text-bg-light">{{ $log->action }}</span></td><td>{{ $log->user?->name ?: 'System' }}<div class="small text-secondary">{{ $log->user?->email }}</div></td><td>{{ $log->tournament?->name ?: ($log->auditable_type ? class_basename($log->auditable_type).' #'.$log->auditable_id : 'Platform') }}</td><td class="small text-secondary">{{ $log->ip_address ?: '—' }}<div class="text-truncate" style="max-width: 180px;" title="{{ $log->user_agent }}">{{ $log->user_agent ?: 'No user agent' }}</div></td><td><code class="small">{{ $log->metadata ? json_encode($log->metadata, JSON_UNESCAPED_SLASHES) : '—' }}</code></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-secondary">No audit records match the selected filters.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $logs->links() }}</div>
    </div>
</x-app-layout>
