<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3"><div><p class="cricket-kicker mb-2">Platform operations</p><h1 class="display-6 fw-bold mb-2">Tournament fleet</h1><p class="text-secondary mb-0">Monitor every season, registration window, fixture schedule, live match room, and completion state from one platform-wide view.</p></div><a href="{{ route('super-admin.dashboard') }}" class="btn btn-light"><i class="fa-solid fa-arrow-left me-2"></i>Control plane</a></div></x-slot>
    <div class="container pb-5">
        <div class="row g-2 mb-4">@foreach(['draft' => 'Draft', 'registration' => 'Registration', 'ready' => 'Ready', 'live' => 'Live', 'completed' => 'Completed', 'cancelled' => 'Cancelled'] as $status => $label)<div class="col-6 col-md-4 col-xl-2"><a href="{{ route('super-admin.tournaments.index', ['status' => $status]) }}" class="cricket-surface p-3 h-100 text-decoration-none d-block"><div class="small text-secondary">{{ $label }}</div><div class="h3 fw-bold text-dark mb-0">{{ $statusCounts[$status] ?? 0 }}</div></a></div>@endforeach</div>
        <div class="cricket-surface p-4 mb-4"><form method="GET" class="row g-2 align-items-end"><div class="col-lg-7"><label class="form-label small text-secondary">Search tournament fleet</label><input type="search" name="search" value="{{ $search }}" class="form-control" placeholder="Name, season, city, or venue"></div><div class="col-lg-3"><label class="form-label small text-secondary">Lifecycle status</label><select name="status" class="form-select"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected($selectedStatus === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-success flex-grow-1">Filter</button><a href="{{ route('super-admin.tournaments.index') }}" class="btn btn-light">Reset</a></div></form></div>
        <div class="cricket-surface overflow-hidden"><div class="d-flex justify-content-between align-items-center gap-3 p-4 border-bottom"><div><p class="cricket-kicker mb-1">Competition directory</p><h2 class="h4 fw-bold mb-0">All tournaments</h2></div><span class="small text-secondary">{{ $tournaments->total() }} total matches</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Tournament</th><th>Status</th><th>Teams</th><th>Players</th><th>Fixtures</th><th>Matches</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($tournaments as $tournament)<tr><td><div class="fw-semibold">{{ $tournament->name }}</div><div class="small text-secondary">{{ $tournament->season_name ?: 'No season name' }} · {{ $tournament->city ?: $tournament->venue ?: 'Location not set' }}</div></td><td><span class="badge {{ $tournament->status === 'live' ? 'text-bg-success' : ($tournament->status === 'completed' ? 'text-bg-dark' : 'text-bg-light') }}">{{ ucfirst($tournament->status) }}</span><div class="small text-secondary mt-1">{{ $tournament->is_public ? 'Public' : 'Private' }}</div></td><td>{{ $tournament->teams_count }}</td><td>{{ $tournament->tournament_players_count }}</td><td>{{ $tournament->fixtures_count }}</td><td>{{ $tournament->matches_count }}</td><td class="text-end"><a href="{{ route('super-admin.tournaments.show', $tournament) }}" class="btn btn-sm btn-outline-success">Inspect</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-secondary">No tournaments match the selected filters.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $tournaments->links() }}</div>
    </div>
</x-app-layout>
